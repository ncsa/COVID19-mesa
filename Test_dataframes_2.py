import pandas as pd

def mesa_function_extraction:


def update_list(variable_dictionary, list):
    new_Tuple = []
    for variable in variable_dictionary:
        new_Tuple.append((variable_dictionary[variable])[0](*(variable_dictionary[variable])[1]))
    list.append(new_Tuple)#?

def


list  = []
for agent in agent_list:
    for variable in variable_list:
        list.append()



List of List_Tuples(Elements in the row of the table)

